# Computational package

The `state_constrained.aligned` package implements the monotone centered
operator, bounded penalties, matched greedy improvement, neural evaluation,
and finite-grid reference solvers used by the experiments.

Install from the repository root with `python -m pip install -e "./code[aligned]"`.
See the [repository README](../README.md) for the selected configurations,
artifact download, tests, and complete reproduction commands.
